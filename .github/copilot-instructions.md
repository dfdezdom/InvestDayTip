# Repository instructions for Copilot

## Current repository state

This repository is currently empty aside from Git metadata. There is no application code, package manifest, build configuration, test suite, or project documentation checked in yet.

## Build, test, and lint

No build, test, or lint commands are defined in the repository at this time.

There is also no single-test workflow available yet because no test runner or test files are present.

Before proposing commands in a future session, re-scan the repository for newly added manifests such as `package.json`, `pyproject.toml`, `Cargo.toml`, `go.mod`, `Makefile`, or CI configuration.

## High-level architecture

No application architecture exists in the repository yet. Treat the project as uninitialized until source files, entry points, or service boundaries are added.

When code is introduced, infer the architecture from the checked-in entry points, runtime configuration, and tests rather than assuming a default stack.

## Key conventions

There are no repository-specific coding conventions or framework patterns established yet.

Do not assume JavaScript, Python, Go, or any other ecosystem until the corresponding project files are present.

If future sessions add tooling or documentation, update this file to capture the real commands, architecture, and conventions instead of leaving placeholder guidance.
